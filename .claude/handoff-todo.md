## Remaining

- Compact `memory/MEMORY.md` — the gitlore hook reports it at 81% of the
  25600-byte budget, with `ddaanet/feedback_posttooluse_print_mode.md` (623
  bytes) and `ddaanet/reference_git_stderr_and_parsing.md` (456 bytes) the
  worst offenders. Trim the longest entries by moving detail into the topic
  files, not by mechanically shortening every line — index lines carry
  ambient awareness, not just pointers.
